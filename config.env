CONFIG_ENV=config.env

# =========================================================
# 1. 源码：根据你的截图，选用 lineage-20
# (截图证实这个分支存在，且比 21 旧一点，稳定性更好)
# =========================================================
KERNEL_SOURCE=https://github.com/LineageOS/android_kernel_xiaomi_sdm710
KERNEL_SOURCE_BRANCH=lineage-20

# 2. 配置文件
# 继续使用 perf (高性能) 配置
KERNEL_CONFIG=vendor/sdm670_perf_defconfig

# 3. 镜像名称
KERNEL_IMAGE_NAME=Image.gz-dtb
ARCH=arm64
ADD_LOCALVERSION_TO_FILENAME=false

# =========================================================
# 4. ⚠️ 编译器：Azure-Clang 14
# LineageOS 20 时代的源码配合 Clang 14 是绝配
# =========================================================
USE_CUSTOM_CLANG=true
CUSTOM_CLANG_SOURCE=https://github.com/Panchajanya1999/azure-clang
CUSTOM_CLANG_BRANCH=main
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

# 清空旧设置
CLANG_BRANCH=
CLANG_VERSION=

# =========================================================
# 5. KernelSU 设置 (开启)
# =========================================================
ENABLE_KERNELSU=true
KERNELSU_TAG=main
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=

# =========================================================
# 6. ⚠️ 绝对防御：强制关闭 Trace 功能
# 这次我们一口气把 FTRACE 和 SCHED_TRACER 全关了
# 这样绝对不会再报 "Unsupported NR_CPUS" 的错误
# =========================================================
EXTRA_CMDS:LD=ld.lld CONFIG_SCHED_TRACER=n CONFIG_FTRACE=n

# 7. 防报错设置
DISABLE-LTO=true
DISABLE_CC_WERROR=true
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true
APPLY_KSU_PATCH=true
REMOVE_UNUSED_PACKAGES=true

# 其他
ENABLE_GCC_ARM64=true
ENABLE_GCC_ARM32=true
USE_CUSTOM_GCC_64=false
USE_CUSTOM_GCC_32=false
USE_CUSTOM_ANYKERNEL3=false
ENABLE_CCACHE=true
NEED_DTBO=false
BUILD_BOOT_IMG=false
SOURCE_BOOT_IMAGE=
