CONFIG_ENV=config.env

# 1. 源码地址 (LineageOS SDM670 - 适配 Android 14/15)
KERNEL_SOURCE=https://github.com/LineageOS/android_kernel_xiaomi_sdm670
KERNEL_SOURCE_BRANCH=lineage-21
KERNEL_CONFIG=vendor/sirius_defconfig
KERNEL_IMAGE_NAME=Image.gz-dtb
ARCH=arm64
ADD_LOCALVERSION_TO_FILENAME=false
EXTRA_CMDS:LD=ld.lld

# =========================================================
# 2. 编译器设置 (改为最稳的 Proton-Clang)
# =========================================================

# 开启自定义编译器
USE_CUSTOM_CLANG=true

# 使用 kdrag0n 的 Proton Clang (业内最稳，主分支 master 绝对存在)
CUSTOM_CLANG_SOURCE=https://github.com/kdrag0n/proton-clang
CUSTOM_CLANG_BRANCH=master

# 交叉编译前缀 (不要动)
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

# 原版 AOSP 设置 (留空失效)
CLANG_BRANCH=
CLANG_VERSION=

# =========================================================

# GCC 设置 (保持开启，防止部分旧代码依赖 GCC)
ENABLE_GCC_ARM64=true
ENABLE_GCC_ARM32=true
USE_CUSTOM_GCC_64=false
CUSTOM_GCC_64_SOURCE=
CUSTOM_GCC_64_BRANCH=
CUSTOM_GCC_64_BIN=aarch64-linux-android-
USE_CUSTOM_GCC_32=false
CUSTOM_GCC_32_SOURCE=
CUSTOM_GCC_32_BRANCH=
CUSTOM_GCC_32_BIN=arm-linux-androideabi-

# 3. KernelSU 设置 (开启)
ENABLE_KERNELSU=true
KERNELSU_TAG=main
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=

# 4. 杂项配置 (关键防报错开关)
# 关闭 LTO (4.9内核开LTO必死)
DISABLE-LTO=true
# 关闭警告转错误 (忽略所有语法警告)
DISABLE_CC_WERROR=true
# 开启 KSU 必需功能
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true
APPLY_KSU_PATCH=true
REMOVE_UNUSED_PACKAGES=true

# 其他设置
USE_CUSTOM_ANYKERNEL3=false
CUSTOM_ANYKERNEL3_SOURCE=
CUSTOM_ANYKERNEL3_BRANCH=
ENABLE_CCACHE=true
NEED_DTBO=false
BUILD_BOOT_IMG=false
SOURCE_BOOT_IMAGE=
