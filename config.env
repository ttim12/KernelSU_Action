CONFIG_ENV=config.env

# =========================================================
# 1. 源码大换血：改用 PixelExperience (PE) 源码
# PE 的源码通常默认就修复了 NR_CPUS 这种弱智报错
# =========================================================
KERNEL_SOURCE=https://github.com/PixelExperience-Devices/kernel_xiaomi_sdm710
KERNEL_SOURCE_BRANCH=thirteen

# 2. 配置文件
# 依然尝试高性能配置，如果 PE 里没有这个文件，Action 会自动回退到通用配置
KERNEL_CONFIG=vendor/sdm670_perf_defconfig

# 3. 镜像名称
KERNEL_IMAGE_NAME=Image.gz-dtb
ARCH=arm64
ADD_LOCALVERSION_TO_FILENAME=false

# 4. 编译器 (继续用 Proton-Clang，这个没问题)
USE_CUSTOM_CLANG=true
CUSTOM_CLANG_SOURCE=https://github.com/kdrag0n/proton-clang
CUSTOM_CLANG_BRANCH=master
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

# 旧设置留空
CLANG_BRANCH=
CLANG_VERSION=

# =========================================================
# 5. ⚠️ 绝杀补丁：通过环境变量强行把 CPU 数量按在地上摩擦
# 我们利用 KCFLAGS 强行定义 NR_CPUS 为 8，试图绕过那个报错检查
# =========================================================
EXTRA_CMDS:KCFLAGS=-DNR_CPUS=8

# 6. KernelSU (开启)
ENABLE_KERNELSU=true
KERNELSU_TAG=main
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=

# 7. 防报错全家桶
DISABLE-LTO=true
DISABLE_CC_WERROR=true
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true
APPLY_KSU_PATCH=true
REMOVE_UNUSED_PACKAGES=true

# 其他
ENABLE_GCC_ARM64=true
ENABLE_GCC_ARM32=true
USE_CUSTOM_GCC_64=false
USE_CUSTOM_GCC_32=false
USE_CUSTOM_ANYKERNEL3=false
ENABLE_CCACHE=true
NEED_DTBO=false
BUILD_BOOT_IMG=false
SOURCE_BOOT_IMAGE=
