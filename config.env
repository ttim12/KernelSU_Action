CONFIG_ENV=config.env

# 1. 源码：LineageOS 20 (保持不变)
KERNEL_SOURCE=https://github.com/LineageOS/android_kernel_xiaomi_sdm710
KERNEL_SOURCE_BRANCH=lineage-20

# 2. 配置文件
KERNEL_CONFIG=sdm670_defconfig

# 3. 镜像名称
KERNEL_IMAGE_NAME=Image.gz-dtb
ARCH=arm64
ADD_LOCALVERSION_TO_FILENAME=false

# =========================================================
# 4. 编译器：保持 crDroid 的 Clang 11 (经你验证，这个能下载！)
# =========================================================
USE_CUSTOM_CLANG=true
CUSTOM_CLANG_SOURCE=https://github.com/crdroidandroid/android_prebuilts_clang_host_linux-x86_clang-6443078
CUSTOM_CLANG_BRANCH=10.0
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

# 清空旧设置
CLANG_BRANCH=
CLANG_VERSION=

# =========================================================
# 5. ⚠️ 核心修复：添加 KCFLAGS 禁令
# KCFLAGS=-fno-builtin-strrchr
# 这行命令的意思是：禁止编译器自动生成 strrchr 函数调用
# 这直接解决了 "__efistub_strrchr" 报错！
# =========================================================
EXTRA_CMDS:LD=ld.lld CONFIG_SCHED_TRACER=n CONFIG_FTRACE=n KCFLAGS=-fno-builtin-strrchr

# 6. KernelSU 设置 (保持 v0.9.5)
ENABLE_KERNELSU=true
KERNELSU_TAG=v0.9.5
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=

# 7. AnyKernel3 设置 (保持大佬版)
USE_CUSTOM_ANYKERNEL3=true
CUSTOM_ANYKERNEL3_SOURCE=https://github.com/YoriInstitute/AnyKernel3-equuleus
CUSTOM_ANYKERNEL3_BRANCH=master

# 8. 基础防报错设置
DISABLE-LTO=true
DISABLE_CC_WERROR=true
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true
APPLY_KSU_PATCH=true
REMOVE_UNUSED_PACKAGES=true

# 其他
ENABLE_GCC_ARM64=true
ENABLE_GCC_ARM32=true
USE_CUSTOM_GCC_64=false
USE_CUSTOM_GCC_32=false
ENABLE_CCACHE=true
NEED_DTBO=false
BUILD_BOOT_IMG=false
SOURCE_BOOT_IMAGE=
